long marcsCakewalk(vector<int> calorie) {
sort(calorie.begin(), calorie.end(), greater<int>());
long miles = 0;
  for (int i = 0; i < calorie.size(); i++) {
        miles += (long)calorie[i] * (1L << i);
    }
    return miles;
}
